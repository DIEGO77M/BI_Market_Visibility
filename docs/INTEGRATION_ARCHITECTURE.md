# 🔗 Integration Architecture

Visual representation of the development environment integration between Databricks, Visual Studio Code, and GitHub.

---

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         DEVELOPMENT ENVIRONMENT                                  │
│                                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                        Visual Studio Code                                │   │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐     │   │
│  │  │ Python Editor    │  │ Jupyter Notebooks │  │ Databricks Panel │     │   │
│  │  │ - .py files      │  │ - .ipynb files   │  │ - Workspace      │     │   │
│  │  │ - Data utils     │  │ - Interactive    │  │ - DBFS           │     │   │
│  │  │ - Tests          │  │ - Cell execution │  │ - Clusters       │     │   │
│  │  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘     │   │
│  │           │                     │                     │                 │   │
│  │           └─────────────────────┴─────────────────────┘                 │   │
│  │                                 │                                        │   │
│  └─────────────────────────────────┼────────────────────────────────────────┘   │
│                                    │                                            │
└────────────────────────────────────┼────────────────────────────────────────────┘
                                     │
          ┌──────────────────────────┼──────────────────────────┐
          │                          │                           │
          ▼                          ▼                           ▼
┌─────────────────────┐   ┌─────────────────────┐   ┌──────────────────────┐
│   LOCAL FILE        │   │   DATABRICKS        │   │   GITHUB             │
│   SYSTEM            │   │   CLOUD             │   │   REPOSITORY         │
│                     │   │                     │   │                      │
│  D:\Projects\       │   │  Workspace:         │   │  DIEGO77M/           │
│  BI_Market_         │   │  /Users/diego../    │   │  BI_Market_          │
│  Visibility/        │   │  .bundle/BI_../     │   │  Visibility          │
│                     │   │                     │   │                      │
│  ├── notebooks/     │◄──┼──├── files/         │   │  ├── main branch    │
│  ├── src/           │   │  │   ├── notebooks/ │   │  ├── develop branch │
│  ├── data/          │───┼─►│   ├── src/       │   │  └── feature/       │
│  ├── dashboards/    │   │  │   ├── data/      │   │     branches        │
│  └── docs/          │   │  │   └── docs/      │   │                      │
│                     │   │  │                  │   │  Protected rules:    │
│  .databrickscfg     │   │  └── state/         │   │  - PR required       │
│  .gitignore         │   │      artifacts/     │   │  - CI/CD checks      │
│  databricks.yml     │   │                     │   │  - Code review       │
└──────────┬──────────┘   └──────────┬──────────┘   └──────────┬───────────┘
           │                         │                           │
           │  databricks            │                           │
           │  bundle deploy         │    git push               │
           └────────────────────────►│◄──────────────────────────┘
                                     │
                                     │
                                     ▼
                        ┌─────────────────────────┐
                        │   DATABRICKS CLUSTER    │
                        │                         │
                        │  ├── Bronze Layer       │
                        │  │   Raw data ingestion │
                        │  │                      │
                        │  ├── Silver Layer       │
                        │  │   Cleaned & validated│
                        │  │                      │
                        │  └── Gold Layer         │
                        │      Business analytics │
                        │                         │
                        │  Data Storage:          │
                        │  - Delta Tables         │
                        │  - DBFS                 │
                        │  - Unity Catalog        │
                        └─────────────────────────┘
```

---

## 🔄 Data Flow Sequence

```
┌──────────┐
│Developer │
└────┬─────┘
     │
     │ 1. Write code in VS Code
     ▼
┌─────────────────┐
│ Local Notebook  │
│  (Python/SQL)   │
└────┬────────────┘
     │
     │ 2. databricks bundle deploy
     ▼
┌─────────────────────┐
│ Databricks Workspace│
│   (Remote Files)    │
└────┬────────────────┘
     │
     │ 3. Execute on Cluster
     ▼
┌──────────────────┐       ┌─────────────────┐
│ Spark Processing │──────►│ Delta Tables    │
│ - Bronze Layer   │       │ - Bronze        │
│ - Silver Layer   │       │ - Silver        │
│ - Gold Layer     │       │ - Gold          │
└──────────────────┘       └─────────────────┘
                                    │
                                    │ 4. Consume data
                                    ▼
                           ┌─────────────────┐
                           │  Power BI       │
                           │  Dashboard      │
                           └─────────────────┘
```

---

## 🔐 Authentication & Security Flow

```
┌──────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION CHAIN                       │
└──────────────────────────────────────────────────────────────┘

1. DATABRICKS AUTHENTICATION
   ┌─────────────────────┐
   │ User (Developer)    │
   └──────────┬──────────┘
              │
              │ Generates Personal Access Token
              ▼
   ┌─────────────────────────────┐
   │ Databricks User Settings    │
   │ - Developer → Access Tokens │
   └──────────┬──────────────────┘
              │
              │ Token stored in
              ▼
   ┌─────────────────────────┐
   │ .databrickscfg          │
   │ ~/.databrickscfg        │
   │ [DEFAULT]               │
   │ host = https://...      │
   │ token = dapi123...      │
   └──────────┬──────────────┘
              │
              │ Used by
              ▼
   ┌─────────────────────────┐
   │ Databricks CLI          │
   │ VS Code Extension       │
   └─────────────────────────┘

2. GITHUB AUTHENTICATION
   ┌─────────────────────┐
   │ User (Developer)    │
   └──────────┬──────────┘
              │
              │ First push triggers
              ▼
   ┌─────────────────────────┐
   │ Git Credential Manager  │
   │ (Windows/macOS/Linux)   │
   └──────────┬──────────────┘
              │
              │ Opens browser for OAuth
              ▼
   ┌─────────────────────────┐
   │ GitHub OAuth Flow       │
   │ - Authorize VS Code     │
   │ - Grant permissions     │
   └──────────┬──────────────┘
              │
              │ Token cached
              ▼
   ┌─────────────────────────┐
   │ Local Git Config        │
   │ Credential helper       │
   └─────────────────────────┘

3. SECURITY BEST PRACTICES
   ┌─────────────────────────────────────────┐
   │ ✅ DO:                                  │
   │ - Use .gitignore for .databrickscfg     │
   │ - Use environment variables for secrets │
   │ - Rotate tokens every 90 days           │
   │ - Use branch protection rules           │
   │ - Enable 2FA on GitHub                  │
   │                                          │
   │ ❌ DON'T:                               │
   │ - Commit tokens to Git                  │
   │ - Share .databrickscfg files            │
   │ - Use tokens in code directly           │
   │ - Disable security features             │
   └─────────────────────────────────────────┘
```

---

## 📊 CI/CD Pipeline Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         GitHub Actions Workflow                          │
└─────────────────────────────────────────────────────────────────────────┘

TRIGGER: git push to main/develop
         │
         ▼
┌─────────────────────┐
│ GitHub Actions      │
│ Workflow Starts     │
└─────┬───────────────┘
      │
      ├──► JOB 1: TEST (Matrix: Python 3.8, 3.9, 3.10)
      │    ┌────────────────────────┐
      │    │ 1. Checkout code       │
      │    │ 2. Setup Python        │
      │    │ 3. Cache dependencies  │
      │    │ 4. Install packages    │
      │    │ 5. Run pytest          │
      │    │ 6. Upload coverage     │
      │    └────────┬───────────────┘
      │             │
      │             ├─► ✅ PASS → Continue
      │             └─► ❌ FAIL → Block PR
      │
      ├──► JOB 2: LINT
      │    ┌────────────────────────┐
      │    │ 1. Checkout code       │
      │    │ 2. Setup Python        │
      │    │ 3. Run flake8          │
      │    │ 4. Check black format  │
      │    │ 5. Check isort         │
      │    └────────┬───────────────┘
      │             │
      │             ├─► ✅ PASS → Continue
      │             └─► ❌ FAIL → Block PR
      │
      └──► JOB 3: SECURITY
           ┌────────────────────────┐
           │ 1. Checkout code       │
           │ 2. Setup Python        │
           │ 3. Run safety check    │
           │ 4. Run bandit          │
           │ 5. Upload reports      │
           └────────┬───────────────┘
                    │
                    ├─► ✅ PASS → Allow merge
                    └─► ⚠️ WARNING → Review required
                         │
                         ▼
                    ┌──────────────────┐
                    │ PR Status Checks │
                    │ All passed ✅    │
                    └──────────────────┘
                         │
                         │ Merge to main
                         ▼
                    ┌──────────────────┐
                    │ Main Branch      │
                    │ Updated          │
                    └──────────────────┘
```

---

## 🗂️ File Sync Strategy

```
LOCAL (VS Code)                    DATABRICKS                   GITHUB
─────────────────                  ──────────                   ──────

notebooks/                         Workspace:/...                main branch
├── 01_bronze.py     ──deploy──►  ├── files/      ◄──watch──   ├── notebooks/
├── 02_silver.py                   │   ├── notebooks/           ├── src/
└── 03_gold.py                     │   ├── src/                 └── docs/
                                   │   └── data/
src/                               │                            develop branch
├── utils/           ──deploy──►  └── state/                   ├── feature branches
└── tests/                             ├── lock.json            └── PR review
                                       └── artifacts/
data/
├── raw/             (not synced)   DBFS:                       .gitignore
├── bronze/          ◄───────────   /mnt/datalake/              excludes:
├── silver/                         ├── bronze/                 - Large data
└── gold/                           ├── silver/                 - Credentials
                                    └── gold/                   - Temp files

┌─────────────────────────────────────────────────────────────────┐
│ SYNC COMMANDS:                                                   │
│                                                                  │
│ Local → Databricks:                                            │
│   databricks bundle deploy                                      │
│                                                                  │
│ Local → GitHub:                                                 │
│   git add . && git commit -m "msg" && git push                  │
│                                                                  │
│ Databricks → Local:                                            │
│   databricks workspace export <remote_path> <local_path>        │
│                                                                  │
│ GitHub → Local:                                                 │
│   git pull origin main                                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Key Integration Points

### 1. **VS Code ↔ Databricks**
- **Extension:** `databricks.databricks`
- **Authentication:** Personal Access Token in `.databrickscfg`
- **Deployment:** `databricks bundle deploy`
- **File Access:** Workspace browser in VS Code sidebar

### 2. **VS Code ↔ GitHub**
- **Protocol:** Git over HTTPS with OAuth
- **Authentication:** Git Credential Manager
- **Operations:** Push, Pull, PR creation
- **CI/CD:** GitHub Actions on push events

### 3. **Databricks ↔ GitHub**
- **Indirect:** Via local repository
- **Best Practice:** Code in GitHub, execution in Databricks
- **Repos Integration:** (Optional) Databricks Repos can sync with GitHub

---

## ✅ Integration Checklist

| Component | Status | Verification Command |
|-----------|--------|---------------------|
| Databricks CLI | ✅ | `databricks --version` |
| Databricks Auth | ✅ | `databricks workspace list /Users/...` |
| VS Code Databricks | ✅ | Check sidebar icon |
| Git Repository | ✅ | `git status` |
| GitHub Remote | ✅ | `git remote -v` |
| CI/CD Pipeline | ✅ | Check GitHub Actions tab |
| Branch Protection | ✅ | Try pushing to main directly |
| Bundle Config | ✅ | `databricks bundle validate` |

---

**📚 Related Documentation:**
- [Development Setup Guide](DEVELOPMENT_SETUP.md)
- [Architecture Design](architecture/README.md)
- [Data Dictionary](data_dictionary.md)
- [Contributing Guidelines](../CONTRIBUTING.md)
